/*
    private static void calcularResultado(JTextField pantalla) {
        boolean operacionSuma;
        if (operacionSuma = true) {
            double resultado;
            resultado = Double.parseDouble(pantalla.getText() + pantalla.getText());
            String numCadena = String.valueOf(resultado);
            pantalla.setText(numCadena);
        }
    }
*/